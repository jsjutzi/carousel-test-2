import React, { ReactElement } from 'react';
import Carousel from './Components/Carousel';
import './App.css';

const App = (): ReactElement => {
    return (
        <div className="App">
            <Carousel />
        </div>
    );
};

export default App;
